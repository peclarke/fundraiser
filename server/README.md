> Note: in an ideal world, this should be run within docker compose

# Server
Start the development server with `npm run dev`.
 
Please look at the `package.json` scripts for more commands about building and testing.